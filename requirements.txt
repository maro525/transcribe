numpy==1.26.4
numba==0.60.0
more-itertools
tiktoken
tqdm
pyannote.audio==3.1.1
python-dotenv==1.0.1
onnxruntime==1.20.1
torch
torchaudio
openai-whisper
fastapi
uvicorn[standard]
jinja2

# Live mode (realtime transcription) — batch pipeline deps above are unchanged
pywhispercpp
silero-vad
# Moonshine CPU live engine (LIVE_ENGINE=moonshine) — reuses the existing
# torch install, CUDA not required
transformers>=4.52,<5
